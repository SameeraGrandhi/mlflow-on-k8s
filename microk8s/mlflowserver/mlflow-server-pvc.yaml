apiVersion: v1
kind: PersistentVolumeClaim
metadata:
   name: mlflow-pv-claim
   namespace: mlflowonkube
   labels:
      app: mlflowserver
spec:
   storageClassName: longhorn
   accessModes:
   - ReadWriteOnce
   resources:
      requests:
         storage: 1Gi
